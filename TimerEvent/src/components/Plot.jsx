import plotImage from '../assets/plot.png';

const Plot = () => {
    return (
        <div>
            <img className="plotimage" src={plotImage}></img>
        </div>
    );
};

export default Plot;